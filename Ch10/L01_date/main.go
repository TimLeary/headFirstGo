package main

func main()  {
	/*
	date := Date{}
	err := date.SetYear(2019)
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(date.Year)

	err = date.SetMonth(5)
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(date.Month)

	err = date.SetDay(27)
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(date.Day)
	 */
}
